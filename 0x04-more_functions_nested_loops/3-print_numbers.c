#include "main.h"
/**
 * print_numbers - Program that identifies int 0-9
 * Return: x the miltiple of and b
 */

void print_numbers(void)
{
	int x;

	for (x = 48; x <= 57; x++)
	{
	_putchar (x);
	}
	_putchar (10);
}
